@model DemoWebApplication.Models.Login

@{
    ViewData["Title"] = "Index";
}
@* @section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            document.getElementById("registerNewUserBtn").addEventListener("click", handleButtonClick);
        });

        function readCookie(name) {
            var nameEQ = name + "=";
            var cookies = document.cookie.split(';');
            for (var i = 0; i < cookies.length; i++) {
                var cookie = cookies[i];
                while (cookie.charAt(0) == ' ') {
                    cookie = cookie.substring(1, cookie.length);
                }
                if (cookie.indexOf(nameEQ) == 0) {
                    return cookie.substring(nameEQ.length, cookie.length);
                }
            }
            return null;
        }

        function handleButtonClick() {
            // var jwtToken = readCookie('JwtToken');
            // console.log("JWT Token:", jwtToken);
            var apiUrl = '@Url.Action("RegisterUserForm", "User")';
            if (jwtToken) {
                fetch(apiUrl, {
                    method: 'GET',
                    headers: {
                        'Content-Type': 'application/json',
                        'Authorization': 'Bearer AuthRegisterRedirect' 
                    },
                })
                    .then(response => {
                        console.log(response);
                    })
                    .catch(error => {
                        console.error('Error:', error);
                    });
            } else {
                console.error('JWT token cookie not found');
            }
        }
    </script>
} *@


<h1>Login</h1>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Index">
            <input type="hidden" name="ReturnUrl" value="@ViewData["ReturnUrl"]" />
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Username" class="control-label"></label>
                <input asp-for="Username" class="form-control" id="txtUsername"/>
                <span asp-validation-for="Username" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Password" class="control-label"></label>
                <input asp-for="Password" class="form-control" id="txtPassword"/>
                <span asp-validation-for="Password" class="text-danger"></span>
            </div>
            <div>
                <a id="registerNewUserBtn" asp-action="RegisterUserForm" asp-controller="User" method="get">Create new account</a>
            </div>
            <div class="form-group">
                <button id="loginBtn" type="submit" value="Create" class="btn btn-primary">Submit</button>
            </div>
        </form>
    </div>
</div>
